% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vecshift_input.R
\name{vecshift_input_helpers}
\alias{vecshift_input_helpers}
\title{Internal helpers for the vecshift v2 input contract}
\description{
These helpers centralize the validation that every public entry point
consuming \code{vecshift} output performs. They check that the input is a
\code{data.table}, that it carries the columns the caller actually needs, and
(when \code{vecshift} >= 2.0.0 is installed) that the object is a
\code{vecshift_result} produced with day-level granularity. Class and metadata
are reattached to consolidation outputs so downstream code keeps seeing
the same S3 type.
}
\keyword{internal}
