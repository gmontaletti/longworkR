% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/career_profile.R
\name{career_profile}
\alias{career_profile}
\title{Unified per-person career profile}
\usage{
career_profile(
  data,
  indicators = "core",
  quality_source = "survival",
  reference_date = NULL
)
}
\arguments{
\item{data}{A \code{data.table} (or \code{vecshift_result}) produced by
\code{vecshift::vecshift()}. Must contain at least \code{cf}, \code{inizio}, \code{fine},
\code{durata}, \code{arco}, \code{over_id}, and \code{prior}.}

\item{indicators}{Character vector. Which indicator groups to include.
\code{"core"} is always returned. Valid values:
\code{c("core", "stability", "complexity", "transitions", "wages", "all")}.
\code{"all"} expands to all four optional groups. Defaults to \code{"core"}.}

\item{quality_source}{Character scalar. How \code{contract_quality} is
computed. Only \code{"survival"} is accepted in v0.9.0; any other value
errors with a message flagging the reserved future-release API.}

\item{reference_date}{Optional \code{Date} or date-coercible scalar used as
the right-censoring anchor for \code{employment_rate} and \code{gap_share}.
Defaults to \code{max(data$fine)}.}
}
\value{
A \code{data.table} with one row per \code{cf}, keyed by \code{cf}. Columns
depend on \code{indicators}:

\strong{Core (always returned)}
\describe{
\item{\code{cf}}{Person identifier.}
\item{\code{n_periods}}{Distinct consolidated employment periods
(\code{uniqueN(over_id[arco >= 1])}).}
\item{\code{total_days_employed}}{Total days spent in \code{arco >= 1} spells.}
\item{\code{employment_rate}}{\code{total_days_employed / observation_window},
clamped to \verb{[0, 1]}.}
\item{\code{contract_quality}}{Duration-weighted mean of survival-derived
quality scores per contract type (\code{arco >= 1} rows only).}
\item{\code{intensity}}{Duration-weighted mean of \code{prior} where
\code{arco >= 1}.}
}

\strong{Stability} (\code{"stability"} or \code{"all"})
\describe{
\item{\code{spell_cv}}{Coefficient of variation of per-\code{over_id} total
durations.}
\item{\code{gap_share}}{Share of observation window in unemployment
(\code{arco == 0}).}
\item{\code{max_tenure_days}}{Longest single \code{over_id} cumulative
duration.}
}

\strong{Complexity} (\code{"complexity"} or \code{"all"})
\describe{
\item{\code{n_contract_types}}{Number of distinct contract-type codes.}
\item{\code{employer_hhi}}{Herfindahl index over employer durata-weighted
shares. \code{NA} if no employer column is detected.}
\item{\code{type_entropy}}{Shannon entropy (natural log) of contract-type
duration shares.}
}

\strong{Transitions} (\code{"transitions"} or \code{"all"})
\describe{
\item{\code{n_upward}}{Count of consecutive \code{over_id} pairs where
contract quality increases.}
\item{\code{n_downward}}{Count of consecutive pairs where it decreases.}
\item{\code{quality_slope}}{OLS slope of quality against \code{over_id} rank.
\code{NA} if fewer than two \code{over_id}s.}
}

\strong{Wages} (\code{"wages"} or \code{"all"})
\describe{
\item{\code{wage_mean}}{Duration-weighted mean wage.}
\item{\code{wage_median}}{Duration-weighted median wage
(via \code{matrixStats::weightedMedian}).}
\item{\code{wage_growth}}{\code{(last - first) / first} where the wage is
computed per \code{over_id}; \code{NA} with fewer than two \code{over_id}s.}
}
}
\description{
Computes a tidy per-\code{cf} data.table of career indicators from a
\code{vecshift} output. \code{career_profile()} is the single entry point that
replaces the legacy family of \verb{calculate_career_*_metrics()} functions
in longworkR v0.9.0. It always returns a \emph{core} set of six indicators
and accepts an \code{indicators} vector to opt into optional groups.
}
\details{
Contract quality is derived from survival analysis only: per project
policy (\code{CLAUDE.md} CRITICAL CORRECTION), fixed weights are forbidden.
The shared survival-based lookup is built once via
\code{\link[=contract_quality_lookup]{.build_contract_quality_lookup()}} and
reused across all indicator groups.
}
\section{Boundary with \code{analyze_employment_transitions()}}{

The \code{"transitions"} group returns only per-\code{cf} \emph{summaries}
(\code{n_upward}, \code{n_downward}, \code{quality_slope}). Full pairwise transition
matrices and network objects remain the responsibility of
\code{\link[=analyze_employment_transitions]{analyze_employment_transitions()}}.
}

\section{Class preservation}{

\code{career_profile()} is an aggregation: it collapses the input to one
row per \code{cf}. The result is therefore a plain \code{data.table} (keyed by
\code{cf}), even when the input carries the \code{vecshift_result} S3 class.
Period-level vecshift metadata does not apply to the aggregated output.
}

\examples{
\dontrun{
sample_data <- readRDS(system.file("extdata", "sample.rds",
                                   package = "longworkR"))

# Core only
core <- career_profile(sample_data)

# Core + stability
stab <- career_profile(sample_data, indicators = c("core", "stability"))

# Everything
full <- career_profile(sample_data, indicators = "all")
}

}
\seealso{
\code{\link[=analyze_employment_transitions]{analyze_employment_transitions()}} for full transition
matrices, \code{\link[=estimate_contract_survival_optimized]{estimate_contract_survival_optimized()}} for the underlying
survival estimator.
}
