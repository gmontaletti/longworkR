% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consolidate_employment_fast.R
\name{consolidate_by_employer_fast}
\alias{consolidate_by_employer_fast}
\title{High-Performance Employment Consolidation by Employer}
\usage{
consolidate_by_employer_fast(
  data,
  employer_var,
  min_lag = 8,
  remove_intermediate_unemployment = TRUE,
  calculate_coverage = TRUE,
  show_progress = FALSE,
  parallel = NULL,
  n_cores = NULL
)
}
\arguments{
\item{data}{data.table with employment records from vecshift output}

\item{employer_var}{Character string specifying employer column name}

\item{min_lag}{Numeric days threshold between contracts (default: 8)}

\item{remove_intermediate_unemployment}{Logical. If TRUE (default), unemployment
periods between consolidated employment periods are removed.}

\item{calculate_coverage}{Logical. If TRUE (default), calculates consolidation_coverage
ratio (employment_days / total_elapsed_days).}

\item{show_progress}{Logical. Display progress messages (default: FALSE)}

\item{parallel}{Logical. Use parallel processing (default: TRUE for >10000 rows)}

\item{n_cores}{Integer. Number of cores for parallel processing (default: detectCores() - 1)}
}
\value{
data.table with consolidated employment periods including metrics:
\itemize{
\item{\code{consolidation_coverage}}: Ratio of employment days to total elapsed days
\item{\code{unemployment_days_removed}}: Days of unemployment removed
\item{\code{n_periods_consolidated}}: Number of original records consolidated
\item{\code{n_unemployment_periods_removed}}: Number of unemployment periods removed
}
}
\description{
Optimized version of consolidate_by_employer() with significant performance improvements:
\itemize{
\item Candidate preselection to process only relevant records
\item Parallel processing support for large datasets
\item Optimized data.table operations with minimal copying
\item Vectorized grouping logic
}
}
\examples{
\dontrun{
# Fast consolidation with automatic parallelization
consolidated <- consolidate_by_employer_fast(data, "datore", 30)

# Force sequential processing
consolidated <- consolidate_by_employer_fast(
  data, "employer_id", 7,
  parallel = FALSE
)

# Custom parallel configuration
consolidated <- consolidate_by_employer_fast(
  data, "company", 90,
  parallel = TRUE,
  n_cores = 4
)
}
}
