% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consolidate_overlapping.R
\name{consolidate_overlapping}
\alias{consolidate_overlapping}
\title{Consolidate Overlapping Employment Periods}
\usage{
consolidate_overlapping(data, variable_handling = "weight")
}
\arguments{
\item{data}{A data.table containing employment periods processed by vecshift,
with columns: \code{cf} (person ID), \code{inizio} (start date), \code{fine} (end date),
\code{durata} (duration), \code{over_id} (overlapping period identifier), and optionally
\code{arco} (employment indicator).}

\item{variable_handling}{Character string specifying aggregation strategy for variables:
\code{"weight"} uses weighted mean/mode (default), \code{"first"} takes first non-NA value}
}
\value{
A data.table with consolidated employment periods, where:
\itemize{
\item Periods with the same \code{over_id > 0} are merged into single periods
\item \code{inizio} is the earliest start date in the group
\item \code{fine} is the latest end date in the group
\item \code{durata} is recalculated as \code{fine - inizio + 1}
\item \code{n_periods_consolidated} indicates how many periods were merged
\item Qualitative variables use weighted mode (most frequent by duration)
\item Quantitative variables use weighted mean
\item Original column types are preserved
}
}
\description{
Merges concurrent employment periods (multiple jobs held at the same time).
Identifies overlapping employment using the \code{over_id} column from vecshift
processing and consolidates them into single periods with aggregated attributes.
}
\details{
This function is designed for employment data where concurrent jobs are
identified by vecshift's \code{over_id} column. All periods sharing the same
\code{cf} and \code{over_id > 0} are considered overlapping and consolidated.

\strong{Consolidation rules}:
\itemize{
\item Employment periods with \code{over_id > 0}: grouped by \code{cf} and \code{over_id}
\item Other periods (unemployment, single jobs): kept as-is (unique group per record)
\item If \code{arco} column is missing, it's created (1 when \code{over_id > 0}, 0 otherwise)
}

\strong{Aggregation by column type}:
\itemize{
\item \strong{Dates} (\code{inizio}, \code{fine}): min/max across group
\item \strong{Duration} (\code{durata}): recomputed as \code{fine - inizio + 1}
\item \strong{Numeric/Integer}: weighted mean (preserves integer type)
\item \strong{Character/Factor}: weighted mode (sum durations by value, pick max)
\item \strong{Logical}: majority rule (mean >= 0.5)
}

\strong{Special columns}:
\itemize{
\item \code{arco}: maximum value in group
\item \code{over_id}: first non-zero value (or first if all zero)
\item \code{stato}: preferentially selects employment states when \code{arco > 0}
}

\strong{Performance:}

This function is fully vectorized and optimized for performance:
\itemize{
\item Handles 10M+ employment records efficiently
\item 9x faster than previous consolidation implementations (Phase 3)
\item Phase 4 optimization: 1.2-3x additional speedup via single-period worker bypass
\item Memory efficient: < 1x input data size
\item Base throughput: ~41,000 records/second (Phase 3)
\item Optimized throughput: ~50,000-120,000 records/second (Phase 4, dataset dependent)
}

Phase 4 automatically skips consolidation for single-period workers with over_id == 0
(no overlapping possible). Performance scales with percentage of such workers.

\strong{Composability:}

Designed to be the first step in a consolidation chain. After merging
concurrent employment, you typically want to merge adjacent periods and
optionally bridge short gaps:

\preformatted{
data |>
  consolidate_overlapping() |>  # Step 1: Merge concurrent jobs
  consolidate_adjacent() |>     # Step 2: Merge touching periods
  consolidate_short_gaps(30)    # Step 3: Bridge short gaps
}
}
\examples{
\dontrun{
# Load sample data
data <- readRDS("data/sample.rds")

# Consolidate overlapping employment periods
consolidated <- consolidate_overlapping(data)

# Check consolidation results
cat("Original records:", nrow(data), "\n")
cat("After consolidation:", nrow(consolidated), "\n")
cat("Periods consolidated:",
    sum(consolidated$n_periods_consolidated > 1, na.rm = TRUE), "\n")

# View a person with overlapping employment
person_data <- data[cf == 165 & over_id > 0]
person_consolidated <- consolidate_overlapping(person_data)

# Chain with other consolidation functions
fully_consolidated <- data |>
  consolidate_overlapping() |>
  consolidate_adjacent()

# Integration with analyze_employment_transitions()
# Pre-consolidate data before transition analysis
consolidated <- consolidate_overlapping(data)
transitions <- analyze_employment_transitions(consolidated)

# Performance with large datasets
large_data <- readRDS("data/large_sample.rds")  # 500K records
system.time({
  result <- consolidate_overlapping(large_data)
})  # Completes in seconds, not minutes
}

}
\seealso{
\code{\link{consolidate_adjacent}} to merge touching employment periods
\code{\link{consolidate_by_employer}} to merge same-employer periods
\code{\link{consolidate_short_gaps}} to bridge short unemployment gaps
}
