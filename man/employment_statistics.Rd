% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/employment_summaries.R
\name{employment_statistics}
\alias{employment_statistics}
\title{Extract employment statistics}
\usage{
employment_statistics(data)
}
\arguments{
\item{data}{A data.table (or data.frame) of consolidated employment spells
containing at least the columns \code{cf} (person identifier), \code{durata}
(spell duration in days), and \code{arco} (concurrent employment indicator,
0 for unemployment spells).}
}
\value{
A named list with elements:
\itemize{
\item \code{total_records}: total number of spells
\item \code{unique_persons}: number of distinct \code{cf} values
\item \code{total_employment_days}: sum of \code{durata} for employment spells
\item \code{total_unemployment_days}: sum of \code{durata} for unemployment spells
\item \code{avg_employment_duration}: mean \code{durata} of employment spells
\item \code{avg_unemployment_duration}: mean \code{durata} of unemployment spells
}
\code{NA} values in \code{durata} are ignored (\code{na.rm = TRUE}).
}
\description{
Computes summary statistics from consolidated employment data produced by
the vecshift/longworkR consolidation chain. Employment and unemployment
spells are distinguished via the \code{arco} indicator (\code{arco == 1} employment,
\code{arco == 0} unemployment); durations are taken from the \code{durata} column.
On consolidated data overlaps are already resolved, so \code{arco} takes only
the values 0 and 1.
}
\examples{
dt <- data.table::data.table(
  cf = c("A", "A", "B"),
  durata = c(100, 30, 200),
  arco = c(1, 0, 1)
)
employment_statistics(dt)

}
