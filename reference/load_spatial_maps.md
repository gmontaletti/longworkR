# Load comuni and CPI spatial data

Loads the comuni and CPI shapefiles from the data/maps directory. These
files contain polygons for Lombardy municipalities and CPI areas.

## Usage

``` r
load_spatial_maps()
```

## Value

A list with two sf objects: comuni and cpi
