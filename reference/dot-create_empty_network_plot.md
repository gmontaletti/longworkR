# Create Empty Network Plot for Error Cases

Internal helper to create informative empty plots when data is
insufficient.

## Usage

``` r
.create_empty_network_plot(message)
```

## Arguments

- message:

  Message to display

## Value

A ggplot2 object with error message
