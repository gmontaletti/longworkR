# Simple temporal consolidation for metrics calculation

Simple temporal consolidation for metrics calculation

## Usage

``` r
.apply_temporal_consolidation_simple(data, cons_type = "both")
```

## Arguments

- data:

  Input data.table

- cons_type:

  Consolidation type

## Value

Consolidated data.table
