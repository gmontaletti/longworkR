# Export to Excel Format

Export to Excel Format

## Usage

``` r
.export_to_xlsx(
  analysis_results,
  summary_tables,
  output_dir,
  file_prefix,
  timestamp_suffix,
  include_raw_data,
  include_summary_tables,
  separate_sheets
)
```
