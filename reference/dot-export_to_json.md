# Export to JSON Format

Export to JSON Format

## Usage

``` r
.export_to_json(
  analysis_results,
  summary_tables,
  output_dir,
  file_prefix,
  timestamp_suffix,
  include_raw_data,
  include_summary_tables
)
```
